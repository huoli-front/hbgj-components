{
  "name": "@hbgj/gateway",
  "version": "0.0.1",
  "description": "gateway util",
  "main": "index.js",
  "typings": "index.d.ts",
  "module": "dist/esm/gateway.js",
  "files": [
    "dist",
    "index.d.ts"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "echo \"Error: no test specified\" && exit 1",
    "lint": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "echo \"Error: no test specified\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "rollup": "^2.38.4"
  }
}
